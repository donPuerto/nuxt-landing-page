import type { Config } from 'tailwindcss'

export default {
  theme: {
    extend: {
      fontFamily: {
        jakarta: ["'Plus Jakarta Sans'", 'sans-serif'],
        inter: ["'Inter'", 'sans-serif'],
      },
    },
  },
} satisfies Config
